const sumar = (a, b) => a + b;
const restart = (a, b) => a - b;

export const divide = (a, b) => a / b;

const multiplica = (a, b) => a * b;


export {sumar as sumarNumber , restart};

export default multiplica;
